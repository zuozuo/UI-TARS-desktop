import { FileEdit, Search, Code, Terminal, Globe, Image } from 'lucide-react';

export const toolIcons = {
  file: FileEdit,
  editor: FileEdit,
  search: Search,
  code: Code,
  terminal: Terminal,
  browser: Globe,
  image: Image,
  default: Code,
};
